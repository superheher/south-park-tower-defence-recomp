# Generated by ReXGlue v0.8.1.4-dev.ge8ce24f on 2026-05-23 06:23:34 UTC.

[project]
name = "south_park_td"
sdk_version = "0.8.1.4"
game_root = "private/extracted"

[entrypoint]
file_path = "private/extracted/default.xex"
out_directory_path = "generated/default"
# NOTE: `generate_exception_handlers = true` was tested and REVERTED — it emits
# SEH_TRY/SEH_CATCH wrappers but (1) rexglue doesn't add the <rex/platform/exceptions.h>
# include so it fails to compile ("undeclared identifier 'SEH_TRY'"), and (2) all 77
# wrappers only log + SEH_RETHROW (no handler dispatch) and RtlUnwind is a no-op stub —
# so it provides crash-reporting, NOT the SEH exception RECOVERY this title needs. Real
# SEH support is unimplemented in rexglue 0.8. See KB 35-entry-forensics.md.
# config/sp_functions.toml: functions rexglue's analyzer misses (vtable methods +
# computed-jump/adjustor-thunk targets) — registered as CONFIG entry points so indirect
# calls resolve. Generated by tools/gen_missing_funcs.py. See KB 35-entry-forensics.md.
includes = ["config/sp_functions.toml"]
